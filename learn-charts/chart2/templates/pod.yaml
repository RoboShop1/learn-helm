apiVersion: v1
kind: Pod
metadata:
  name: pod1
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "-2"
spec:
  restartPolicy: Never
  containers:
    - name: c1
      image: alpine
      command: ["sleep","30"]